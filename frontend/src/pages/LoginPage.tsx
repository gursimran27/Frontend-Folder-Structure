import { Box } from "@mui/material";
import LoginForm from "../components/LoginForm";

const LoginPage = () => {
  return (
    <Box>
      <LoginForm />
    </Box>
  );
};

export default LoginPage;
